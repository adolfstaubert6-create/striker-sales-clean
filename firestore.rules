rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Iba autentifikovaní Firebase Auth používatelia môžu čítať/písať
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
